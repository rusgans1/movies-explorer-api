{ "extends": "airbnb-base",
  "rules": {
    "no-underscore-dangle": ["error", {"allow": ["_id"]}]
  },
  "overrides": [
    {
      "files": ["validation.js"],
      "rules": {
        "no-useless-escape": "off"
      }
    }
  ]
}